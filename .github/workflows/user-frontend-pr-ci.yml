name: user-frontend-pr-ci

on:
  pull_request:
    paths:
      - user-frontend/**
      - .github/workflows/user-frontend-pr-ci.yml
      - .github/workflows/user-frontend-main-ci.yml
      - .github/workflows/user-frontend-nightly-ci.yml
  workflow_dispatch:

concurrency:
  group: user-frontend-pr-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: user-frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: user-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Run build
        run: npm run build
